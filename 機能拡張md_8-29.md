# 機能拡張md 8/29 — 復習画面の整理と一覧ページ化

対象：`index.html`（単一ファイルのアプリ本体）。関連CSSは `src/modern.css`。

## 目的

「復習・苦手」画面（`#reviewView`）を、ホーム画面と同じ構造の入口ページに作り替える。
各項目は画面内に一覧を並べるのではなく、**専用ページへ遷移**させる。

## 要件

1. 復習画面のレイアウトをホーム画面に寄せて整理する
2. 「見直したい問題」→ 別ページで★付き問題の一覧（**1問＝1ページ**）
3. 「苦手問題」→ 別ページで苦手問題の一覧（同じく1問1ページ）
4. 「弱点分野」→ 演習済み問題の**分野別正答率の統計ページ**

## 現状（変更前）

- `#reviewView`：今日の復習カード／採点待ちカード／誤答ノートカード＋
  `見直したい問題`・`苦手問題` が `renderGroupList()` の「1枚のカテゴリカード」として置かれ、
  押すと出題数モーダル→そのまま演習開始。一覧は見られない。
- `弱点プレイリスト` は `PLAYLISTS`（弱点分野／まだ正解していない／最近間違えた）の3枚。
  `弱点分野` を押すと統計ではなく演習が始まる。
- 問題一覧（`#qbrowse`）は 1ページ10問（`QB_PAGE`）で、問題文・選択肢・解説・★・選択演習を持つ。

## 設計

### 1. 復習画面（ホーム型レイアウト）

ホームと同じ構成要素を流用する。

```
reviewTop（← ホーム / 復習・苦手）
homeLead（leadEyebrow + h2 + p ＋ leadStats 3枚：今日の復習／苦手/見直し）
dueCard（今日の復習）        … 既存のまま
dueCard（採点待ちの試験）    … 既存のまま（該当時のみ）
homeMainLabel「復習メニュー」
hubGrid（#reviewHubGrid）… 新規 renderReviewHub() で4枚を生成
  ・見直したい問題 → 一覧ページ
  ・苦手問題       → 一覧ページ
  ・誤答ノート     → 既存 mistakesView
  ・弱点分野       → 新規 weakFieldView
sectlabel「おすすめプレイリスト」＋ plGrid … まだ正解していない／最近間違えた
```

- 既存の `#mistakeEntrySection`（誤答ノートのカード）と、`#bookmarkSection` / `#weakSection`
  （`renderGroupList` のカテゴリカード）は削除し、hubGrid に統合する。
- `PLAYLISTS` から `weakfield` を外す（統計ページへ移動）。`plWeakFields()` 自体は統計ページの
  「弱点分野をまとめて演習」で再利用するので残す。

### 2・3. 見直したい問題／苦手問題の一覧ページ

新画面は作らず、**`#qbrowse` を1問1ページモードで再利用**する（問題文・選択肢・正解・解説・
★トグル・「この問題を解く」・複数選択演習をそのまま使えるため）。

`qbrowseState` に2つ足す：

- `perPage`（既定 `QB_PAGE=10`、復習の一覧は `1`）
- `origin`（`"home"` / `"reviewView"`。戻り先とフッタnavのハイライトに使う）

変更点：

- `renderQBrowseGroup()` の `QB_PAGE` を `qbrowseState.perPage` に置換
- `perPage===1` のときは詳細（`.qbBack`）を最初から開いた状態にする
- 0問のときは空メッセージを出す（遷移はする）
- 戻るラベル／`qbBackStep()`／`navKeyForScreen()` を `origin` で分岐

### 4. 弱点分野（分野別正答率）

新ビュー `#weakFieldView` を追加（`VIEWS` に登録）。

- 集計対象：`progress` に解答記録がある問題（`correct+wrong>0`）のみ。分野は `q.field||"その他"`。
- 表示：分野ごとに 正答率バー（既存 `.fbRow/.fbBar/.fbNum` を流用）・`正解/解答数`・問題数、
  正答率の低い順。正答率50%未満は弱点として色を変える。
- 各行から「この分野を演習」（`openCountModal`）、ヘッダに「弱点分野をまとめて演習」。
- 未解答なら空パネル。

## 変更ファイル

- `index.html`
  - markup: `#reviewView` の作り替え、`#weakFieldView` の追加
  - JS: `renderReviewHub()` / `openReviewList()` / `renderWeakFields()` の追加、
    `qbrowseState`・`renderQBrowseGroup`・`qbBackStep`・`renderQBrowse`・`navKeyForScreen`・
    `VIEWS`・`refreshData`・`PLAYLISTS`・イベント配線の修正
- `src/modern.css`：必要なら弱点分野ページの最小限のスタイル（既存クラスで足りれば追加なし）

## 検証

- 低リスク（表示・遷移のみ、データ構造は不変）。`npm run build` と全テストは回さない。
- 手動確認：復習画面→各項目→一覧のページ送り→★の付け外し→戻る、弱点分野の集計、
  問題0件・未解答時の空表示。

## 残リスク

- `#qbrowse` を共用するため、問題一覧（年度別）側の挙動に副作用が出ないこと（`perPage`/`origin`
  は `openQBrowse()` で毎回リセットする）。

---

## 実装結果（2026-08-29）

すべて実装済み。

### index.html（markup）

- `#reviewView` を作り替え：`homeLead`（3統計）＋今日の復習／採点待ち＋`homeMainLabel`＋
  `#reviewHubGrid`＋おすすめプレイリスト。旧 `#mistakeEntrySection` / `#bookmarkSection` /
  `#weakSection` は削除。
- `#weakFieldView`（`#weakFieldBody`・戻るボタン `#weakFieldBack`）を追加。
- CSS：`.qbPageGap` と弱点分野の `.wfHead/.wfStats/.wfList/.wfRow/.wfTop/.wfName/.wfPct/.wfMeta` を追加。

### index.html（JS）

- 追加：`bookmarkItems()` / `weakItems()` / `fieldAccuracy()` / `renderReviewHub()` /
  `openReviewList(kind)` / `renderWeakFields()` / `qbPageNumbers()`
- 変更：`VIEWS` に `weakFieldView`、`navKeyForScreen()`（復習由来の `qbrowse` は復習タブ）、
  `openView()` の分岐、`refreshData()`（`renderMistakeEntry` を廃止し `renderReviewHub` を呼ぶ）、
  `renderDue()`（`#reviewLeadDue` も更新）、`PLAYLISTS` から `weakfield` を削除、
  `qbrowseState` に `perPage`/`origin`、`renderQBrowse()`／`renderQBrowseGroup()`／`qbBackStep()`／
  `openQBrowse()`、`saveNote()`、`renderScopeLists()` から死んだ `renderGroupList("weak"/"bookmark")` を削除。
- 削除：`renderMistakeEntry()`

### src/navigation.ts

- `weakFieldView: "review"` を追加（フッタnavのハイライト用）。

### 検証

- 巨大インラインスクリプトを抽出して `node --check` → 構文OK
- `npx vitest run tests/navigation.test.ts` → 4 passed
- 重複 id チェック → 新規追加分の重複なし
- `npm run build` / 全テストは未実行（表示と遷移のみの変更のため）
