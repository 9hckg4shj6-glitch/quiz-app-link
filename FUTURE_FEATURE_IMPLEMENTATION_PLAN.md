# 今後の主要3機能 実装計画書

最終更新: 2026-08-26
対象リポジトリ: `問題演習アプリ`

## 進捗（2026-08-26）

| 作業 | 状態 | 備考 |
|---|---|---|
| 初回基盤: Card / Deck追加項目 | 完了 | 休止、共有元、1日の新規・復習上限、目標保持率 |
| IndexedDB v2 → v3移行 | 完了 | 既存データへ既定値を自動補完 |
| バックアップv3 | 完了 | v2の読み込み互換を維持 |
| Supabase同期列 | コード完了・適用待ち | `010_card_learning_settings.sql` を本番DBへ適用後に公開する |
| カード学習ホーム | 完了 | 今日の復習・未学習・自作カード、デッキ別進捗と学習開始 |
| デッキ設定・カード休止UI | 未着手 | 次の実装Issue |

## 1. 目的

次の3機能を、既存のオフライン対応・FSRS・Googleアカウント同期・Supabase基盤を活かして段階的に追加する。

1. Anki相当のカード学習モード（カード作成、忘却曲線、共有デッキ）
2. 科目資料を共有・ダウンロードできる「PDFの溜まり場」
3. 演習中の思考メモと図の書き込み

本計画でいう「Anki相当」は、間隔反復を中心とした学習体験を実現するという意味であり、Ankiの名称、画面、アイコン等を複製するものではない。アプリ内では「カード学習」など独自の名称とUIを使用する。

## 2. 現状調査

### 2.1 すでに利用できる土台

| 領域 | 現在あるもの | 主な実装箇所 |
|---|---|---|
| カード | 作成、編集、複製、削除、デッキ作成、画像、JSONバックアップ | `src/card-manager.ts` |
| 忘却曲線 | FSRS、4段階評価、復習履歴、復習取消、想起率表示 | `src/fsrs.ts`、`index.html` |
| ローカル保存 | カード、デッキ、復習履歴、予定、記述答案 | `src/db.ts` |
| アカウント同期 | Googleログイン、カード画像用Storage、カード・デッキ・復習履歴同期 | `src/sync.ts`、`supabase/migrations/001_initial.sql` |
| 作図 | 記述問題用のペン入力、正規化座標によるストローク保存 | `index.html`、`src/types.ts`、`src/written.ts` |
| 公開投稿 | 掲示板、通報、レート制限、ソフト削除 | `src/community.ts`、`supabase/migrations/003_community.sql` 以降 |

したがって、カード作成と忘却曲線を新規に作り直す必要はない。①は既存機能を独立した「カード学習」体験として整理し、共有デッキを追加する作業が中心となる。③の図入力も、既存の記述問題用キャンバスを共通部品化して再利用できる。

### 2.2 現状との差分

- カード学習の入口、今日の学習量、デッキ別設定、休止カード、学習統計が十分にまとまっていない。
- 共有デッキの公開、検索、取り込み、更新、通報の仕組みがない。
- 演習中に選択問題を含めて自由な思考メモを残す共通UIがない。
- 作図キャンバスは記述答案専用で、メモとして再利用できる構造になっていない。
- PDFのメタデータ、ファイル保存、公開審査、ダウンロード、通報、削除の仕組みがない。

## 3. 全体方針

### 3.1 守る原則

1. **オフライン優先**: 自分のカードとメモはゲストでも使用でき、ログイン後に同期できる。
2. **学習記録を分離**: 共有デッキの内容と、各ユーザーの復習履歴・FSRS予定を混ぜない。
3. **取り込みはコピー方式**: 共有デッキを自分の領域へコピーし、公開者による変更で自分のカードが突然変わらないようにする。
4. **公開ファイルは審査後に表示**: PDFと共有デッキは、アップロード直後に全公開しない。
5. **権利と安全を優先**: 投稿者による配布権限の確認、通報、公開停止、管理者削除、容量制限を最初から入れる。
6. **大容量データをGitへ入れない**: PDFと共有用画像はSupabase Storageへ置き、GitHub Pagesの配信物には含めない。
7. **既存データを壊さない**: DexieとSupabaseのマイグレーションは追加方式とし、既存のカード・復習・答案を維持する。

### 3.2 推奨実装順

| Phase | 内容 | 理由 |
|---|---|---|
| 0 | 共通仕様と運用条件の確定 | 公開範囲、容量、管理方法を先に固定する |
| 1 | 個人向けカード学習の完成 | 既存実装が多く、最短で価値を出せる |
| 2 | 演習メモ（文字） | オフラインのみでも先行公開できる |
| 3 | メモ用作図 | 既存キャンバスを共通化して追加する |
| 4 | 公開・検索・通報の共通基盤 | 共有デッキとPDFの両方で使う |
| 5 | 共有デッキ | 小さなデータで公開基盤を検証できる |
| 6 | PDFの溜まり場 | 容量・権利・セキュリティのリスクが最も高い |
| 7 | 統計、更新通知、運用改善 | 実利用データを見て改善する |

## 4. 機能① カード学習モード

### 4.1 MVPの範囲

- カード学習専用ホーム
- デッキ一覧とデッキ別の「新規」「復習」「学習済み」件数
- 基本カード、用語カード、選択式カードの作成・編集
- 画像添付
- FSRSによる今日の復習キュー
- 4段階評価（忘れた／難しい／できた／簡単）と次回予定の表示
- 直前の評価の取り消し
- カードの一時休止・再開
- デッキごとの1日あたり新規上限・復習上限
- デッキ別の進捗、正答傾向、予測復習数の表示
- JSONによるエクスポート・インポート
- ログイン時の複数端末同期

### 4.2 共有デッキ

#### 公開

- 自作デッキの詳細画面から「共有申請」を行う。
- タイトル、説明、科目、対象学年、タグ、言語、ライセンス・配布権限の確認を必須にする。
- 公開物にはカード内容と必要な共有画像だけを含め、復習履歴、FSRS予定、ユーザー設定は含めない。
- 申請時点の内容を変更不能なバージョンとして保存する。
- 状態は `draft`、`pending`、`published`、`rejected`、`removed` を持つ。
- 管理者確認後に検索結果へ掲載する。初期運用では自動公開しない。

#### 検索・取り込み

- 科目、タグ、更新日、ダウンロード数で絞り込み・並べ替えできる。
- 詳細画面で説明、カード枚数、サンプル、画像枚数、更新履歴を確認できる。
- 「自分のデッキに追加」でローカルDBへコピーし、取り込み後はオフラインで学習できる。
- 同じ公開バージョンの二重取り込みを警告する。
- 新版が公開された場合は通知するが、自動上書きはしない。
- 更新時は「新しいカードのみ追加」「公開版に合わせる」「現在のまま」のいずれかを選べる。MVPでは「新しいカードのみ追加」まででもよい。

#### 学習記録の扱い

- 公開カードのIDと取り込み後の個人カードIDを分ける。
- `originDeckId`、`originVersion`、`originCardId` を保持し、更新照合だけに使用する。
- 復習イベントとFSRS予定は個人カードIDへ記録し、公開側へ送信しない。
- 公開者がデッキを削除しても、すでに取り込んだ個人コピーは残す。

### 4.3 MVP対象外

- `.apkg` の完全互換入出力
- 任意のHTML/CSS/JavaScriptを含むカードテンプレート
- アドオン実行
- 音声・動画の共有
- リアルタイム共同編集
- 公開者から取り込み済みカードへの強制更新

これらは安全性と互換性の検証が大きいため、共有デッキ運用が安定してから別Phaseで検討する。

### 4.4 主なデータ変更案

#### ローカル

- `StudyCard`: `suspendedAt`、`originDeckId`、`originVersion`、`originCardId` を追加
- `Deck`: 新規上限、復習上限、FSRS設定を追加
- `deckSubscriptions`: 取り込んだ公開デッキとバージョンを保存
- `StudyDatabase`: Dexie v3として追加し、既存カードへ安全な既定値を設定

#### Supabase

- `shared_decks`: 公開者、メタデータ、状態、現行バージョン、集計値
- `shared_deck_versions`: バージョン、カード数、変更概要、Storageパス、ハッシュ値
- `shared_deck_reports`: 通報者、理由、状態
- `shared_deck_downloads`: 重複を抑えたダウンロード集計
- `shared-content` Storage bucket: 公開が承認されたデッキパッケージと画像

RLSでは、公開済みデータだけを閲覧可能にし、作成・更新はログイン済みの所有者だけ、状態変更は管理用RPCだけに限定する。

### 4.5 完了条件

- オフラインでカードを作成し、再読込後も失われない。
- FSRSの4評価で予定が変わり、次回起動後も維持される。
- 別端末で同期しても復習イベントが重複・消失しない。
- 公開デッキを取り込んだ後、公開者の更新や削除で個人学習記録が壊れない。
- 未承認・削除済みデッキが公開検索に出ない。
- 通報、公開停止、管理者削除が動作する。

## 5. 機能③ 演習中メモ・図の書き込み

### 5.1 ユーザー体験

- 選択問題・記述問題の両方に、折りたたみ式の「思考メモ」を表示する。
- 解答前に文章や図を書ける。入力は一定間隔と画面遷移時に自動保存する。
- 解答確定時に「解答時点の思考」をスナップショットとして残す。
- 解説表示後は別欄に「振り返り」を追記できる。
- 次回その問題を開いた際、過去メモを日付順に確認できる。
- メモ一覧から科目、問題、日付、本文で検索できる。
- ゲスト時は端末保存、ログイン時はアカウント同期の対象とする。

解答前の内容を後から自由に書き換えると、本来の思考経路が失われる。そのため「編集中の下書き」と「解答時点のスナップショット」を分ける。

### 5.2 テキストメモ

- プレーンテキストをMVPとし、文字数上限を設ける。
- チェックリスト、太字等の装飾は後続Phaseとする。
- 解答前メモと振り返りメモを明確に分ける。
- オートセーブ状態を「保存中」「保存済み」「同期待ち」で表示する。
- メモを書かなくても演習操作を妨げない。

### 5.3 作図

既存の記述問題用キャンバスを、答案とメモの双方が使える共通モジュールへ切り出す。

- ペン、消しゴム、色、太さ
- 1手戻す、やり直す、全消去
- タッチ、Apple Pencil等のPointer Events対応
- 座標は0〜1の正規化値で保存し、画面サイズが変わっても再描画できる
- 問題画像を薄く背景表示して、その上へ書き込める
- MVPではストロークデータを保存し、PNGは常時保存しない
- ストローク数・点数・保存サイズに上限を設ける

### 5.4 データ変更案

#### ローカル

`exerciseNotes` テーブルをDexieへ追加する。

- `id`
- `ownerId`
- `subjectId`
- `questionId`
- `attemptId`（通常演習で不要な場合はnull）
- `status`（`draft` / `submitted`）
- `thinkingText`
- `reflectionText`
- `drawingStrokes`
- `backgroundImageRef`
- `createdAt`、`submittedAt`、`updatedAt`、`syncedAt`、`deletedAt`

下書きは問題単位で1件、提出済みスナップショットは演習回ごとに複数件持てる設計とする。

#### Supabase

- `account_exercise_notes`: 所有者ごとのメモ本文とベクターストローク
- 差分同期用RPC: `(server_updated_at, note_id)` のカーソルでpullし、更新単位でpushする
- RLS: `auth.uid() = owner_id` の本人だけが読み書きできる
- メモは共有データではないため、公開用ポリシーを作らない

同じメモを複数端末で同時編集した場合は、MVPでは `updatedAt` が新しい方を採用する。提出済みスナップショットは更新せず、別レコードとして保持する。

### 5.5 完了条件

- 入力中に再読込・オフライン化しても直近のメモが復元される。
- 解答確定時の思考メモが、その後の振り返り編集で変わらない。
- スマートフォンの縦横切替後も図の位置関係が維持される。
- 連続描画時に演習画面のスクロールや解答操作を妨げない。
- 別アカウントからメモを取得できない。
- メモを全削除した場合、同期先からも削除される。

## 6. 機能② PDFの溜まり場

### 6.1 公開フロー

1. ログイン済みユーザーがPDFとメタデータを送信する。
2. クライアント側で拡張子だけでなくPDFヘッダー、サイズ、ページ数を検査する。
3. 非公開Storage領域へ一時アップロードする。
4. 投稿者が「自作」「配布許可あり」「パブリックドメイン等」の権利区分と出典を申告する。
5. 投稿は `pending` となり、管理者が内容・権利表示・ファイルを確認する。
6. 承認後に `published` とし、検索・ダウンロード対象にする。
7. 通報や権利者申立てがあれば即時非公開にできる。

授業スライド、教科書、過去問には第三者の著作物が含まれる可能性がある。投稿者の申告だけで自動公開せず、配布権限が確認できない資料は公開しない。アプリ内にも禁止事項と削除申請窓口を表示する。

### 6.2 MVPの機能

- PDFアップロード（ログイン必須）
- タイトル、説明、科目、授業名、年度、作成者、タグ、権利区分、出典
- 科目・年度・タグ・キーワード検索
- PDF詳細、ファイルサイズ、ページ数、更新日、ダウンロード数
- 承認済みPDFのダウンロード
- 自分の投稿一覧と公開前の取消
- 不適切・権利侵害・重複・破損ファイルの通報
- 管理者による承認、却下、公開停止、削除
- 同一ファイルのSHA-256による重複警告
- アップロード回数、ファイルサイズ、保存総量の上限

初期上限の案は「PDFのみ、1ファイル50MB、1ユーザー1日5件」。実際のSupabase契約、利用者数、授業資料の平均サイズを確認してリリース前に確定する。

### 6.3 セキュリティ・運用

- MIME type、PDFマジックバイト、ページ数、ファイルサイズを検査する。
- ファイル名をそのままStorageパスに使用せず、サーバー発行IDを使う。
- Storage bucketは非公開とし、承認済みファイルだけ短時間の署名付きURLをRPC経由で取得する。
- ダウンロード可否は表示上のボタンだけでなく、DB/RLS/RPC側でも検証する。
- PDF内のJavaScript、埋め込みファイル、外部リンク等を確認する安全検査工程を設ける。
- ブラウザ内プレビューは安全検査が安定するまでMVP対象外とし、まずダウンロードのみ提供する。
- レート制限、通報重複防止、監査ログ、ソフト削除、管理者操作ログを持つ。
- Storage使用量と転送量に警告値を設定し、上限超過時は新規投稿を停止できるようにする。
- 削除時はメタデータだけでなくStorage上の実ファイルも確実に削除する。

ウイルス・不正PDFの自動検査方式は、Supabaseだけで完結させるか外部スキャナーを使うかをPhase 0で決める。安全な検査を用意できない間は、管理者が確認した限定ユーザーのみアップロード可能とする。

### 6.4 データ変更案

- `pdf_documents`: 投稿者、メタデータ、権利申告、状態、現行ファイル、集計値
- `pdf_files`: Storageパス、サイズ、ページ数、MIME、SHA-256、安全検査結果
- `pdf_reports`: 通報者、理由、詳細、処理状態
- `pdf_downloads`: ダウンロード集計用。公開画面には個人情報を出さない
- `moderation_actions`: 審査・公開停止・削除の監査ログ
- `study-pdfs` Storage bucket: `pending/` と `published/` を論理的に分離

状態変更と署名URL発行は `security definer` RPCへ集約し、入力値、権限、レート、対象状態をサーバー側で再検証する。

### 6.5 MVP対象外

- PDF本文の全文検索
- ブラウザ内の高度なPDFビューア
- PDFへの共同注釈
- OCR
- PDFからの自動カード生成
- 外部AIへのPDF送信
- 匿名ユーザーによるアップロード

### 6.6 完了条件

- 未承認PDFのURLやStorageパスを知っていても第三者が取得できない。
- PDF以外、上限超過、壊れたファイルを登録できない。
- 承認済みPDFだけが一覧・検索・ダウンロードに出る。
- 通報後に管理者が公開停止し、既存のダウンロードURLも短時間で失効する。
- 同一ファイルの重複投稿を検出できる。
- アカウント削除・資料削除時にDBとStorageの孤立データが残らない。

## 7. 共通公開・モデレーション基盤

共有デッキとPDFで、次を共通化する。

- 公開状態: `draft` / `pending` / `published` / `rejected` / `removed`
- 科目、タグ、投稿者、作成日、更新日
- 通報理由と端末・アカウント単位の重複防止
- 管理者による承認、却下、非公開、削除
- 操作ログ
- 一覧のページングとサーバー側検索
- レート制限
- 公開ガイドライン、権利侵害の削除申請導線

既存コミュニティの通報・ソフト削除の考え方は再利用できるが、PDFは法的・容量上の影響が大きいため、「異なる3端末の通報で自動非表示」だけに頼らず、管理者審査と即時公開停止を加える。

## 8. 画面構成案

### 8.1 カード学習

- カード学習ホーム
  - 今日の新規・復習件数
  - デッキ一覧
  - 学習開始
  - カード管理
  - 共有デッキを探す
- デッキ詳細
  - 学習状況
  - カード一覧
  - デッキ設定
  - 共有申請／更新
- 共有デッキ一覧・詳細

### 8.2 演習メモ

- 問題下部の折りたたみパネル
  - 思考メモ
  - 図を書く
  - 保存状態
- 解説表示後の振り返り欄
- 「設定・データ」または「復習」内のメモ一覧

### 8.3 PDFの溜まり場

- PDF一覧・検索
- PDF詳細・ダウンロード
- 投稿フォーム
- 自分の投稿
- 通報フォーム
- 管理者審査画面

## 9. 実装タスク分割

### Phase 0: 仕様・運用確定

- [ ] カード学習のMVP範囲と呼称を確定
- [ ] 公開デッキ・PDFの利用規約と投稿ガイドラインを作成
- [ ] PDFの許可する権利区分と削除申請窓口を確定
- [ ] SupabaseのStorage容量・転送量・バックアップ方針を確認
- [ ] PDF安全検査と管理者認証の方式を決定
- [ ] 機能フラグを設計

### Phase 1: 個人カード学習

- [x] 既存カード機能をカード学習ホームへ統合
- [ ] デッキ別学習キューと上限設定
- [ ] 休止・再開
- [ ] 学習統計
- [x] 型・Dexieマイグレーション・同期更新（SupabaseへのSQL適用はリリース時）
- [x] 既存データ移行テスト

### Phase 2: テキストメモ

- [ ] `ExerciseNote` 型とDexieテーブル
- [ ] 演習画面のメモUI
- [ ] デバウンス付き自動保存
- [ ] 解答時スナップショットと振り返り
- [ ] メモ履歴・検索
- [ ] JSONバックアップ対象への追加

### Phase 3: メモ作図

- [ ] 既存作図コードの共通モジュール化
- [ ] ペン・消しゴム・undo/redo・全消去
- [ ] 背景画像への書き込み
- [ ] 保存上限と縮約処理
- [ ] タッチ端末・画面回転テスト
- [ ] メモ同期

### Phase 4: 公開基盤

- [ ] 共通状態・通報・管理操作のSQLマイグレーション
- [ ] 公開一覧、検索、ページング
- [ ] 管理者審査画面
- [ ] レート制限・監査ログ
- [ ] 公開ガイドラインと削除申請導線

### Phase 5: 共有デッキ

- [ ] 公開用パッケージ形式とバージョン方式
- [ ] 共有申請・審査・公開
- [ ] 検索・サンプル表示・取り込み
- [ ] 由来IDと更新通知
- [ ] 通報・公開停止・削除
- [ ] 別アカウント・削除・更新競合テスト

### Phase 6: PDFの溜まり場

- [ ] PDFメタデータ・Storage・RLS・RPC
- [ ] アップロード前検査とハッシュ計算
- [ ] 安全検査・審査フロー
- [ ] 検索・詳細・署名URLダウンロード
- [ ] 通報・公開停止・完全削除
- [ ] 容量・転送量の監視と投稿停止スイッチ
- [ ] 不正ファイル、直リンク、権限、レート制限テスト

### Phase 7: 運用改善

- [ ] 共有デッキの更新差分表示
- [ ] PDF・共有デッキのお気に入り
- [ ] 検索品質とタグ整理
- [ ] 利用状況に基づく上限調整
- [ ] `.apkg` 互換性の技術調査

## 10. テスト方針

### 単体テスト

- FSRS評価、上限、休止カードのキュー判定
- 公開デッキ取り込み時のID変換と重複防止
- メモのサニタイズ、ストローク上限、スナップショット不変性
- PDFのサイズ、ヘッダー、MIME、ハッシュ検査
- 状態遷移とレート制限

### 結合テスト

- ゲスト作成 → Googleログイン → データ統合
- オフライン編集 → オンライン復帰 → 差分同期
- 2端末でのカード評価・メモ編集
- 公開申請 → 承認 → 検索 → 取り込み／ダウンロード → 通報 → 公開停止
- アカウント削除時のDB・Storage削除

### 手動確認

- iPhone/Androidのタッチ描画、画面回転、PWA再起動
- 長いカード、画像付きカード、大きなデッキ
- 低速回線・アップロード中断・容量不足
- ダークモード、キーボード操作、スクリーンリーダー向けラベル

## 11. リリース方針

各機能は環境変数による機能フラグで隠し、DB適用と運用準備が済むまで公開しない。

1. 開発端末だけで有効化
2. 管理者・少人数テスターで有効化
3. 読み取り中心で公開（共有デッキ閲覧、PDFダウンロード）
4. 投稿者を限定してアップロードを開放
5. 通報・審査・容量の運用を確認後に一般開放

ロールバック時はUIの機能フラグを停止しても、既存の個人カード・メモ・復習履歴を削除しない。公開データはDBの状態変更で非表示にし、緊急時だけStorageアクセスを停止する。

## 12. 工数の目安

既存コードを理解している実装者1名、デザイン作成と規約の法的確認を除く概算。

| Phase | 概算 |
|---|---:|
| 0. 仕様・運用確定 | 2〜4人日 |
| 1. 個人カード学習 | 6〜10人日 |
| 2〜3. メモ・作図 | 8〜13人日 |
| 4. 公開・審査基盤 | 6〜10人日 |
| 5. 共有デッキ | 7〜12人日 |
| 6. PDFの溜まり場 | 12〜20人日 |
| 7. 安定化・運用改善 | 5〜10人日 |
| **合計** | **46〜79人日** |

PDFの安全検査、権利確認、管理画面の要件によって大きく変動する。3機能を同時公開せず、Phaseごとに検証・公開する。

## 13. 最初に着手する具体的なIssue

1. [完了] `Card` / `Deck` の追加項目とDexie v3移行仕様を確定する。
2. [完了] カード学習ホームのワイヤーフレーム作成と初期実装を行う。
3. 既存カード・FSRS機能の不足項目を画面単位で棚卸しする。
4. `ExerciseNote` と「解答時スナップショット」の型を確定する。
5. 記述答案キャンバスから再利用可能な責務を切り出す設計を作る。
6. 公開コンテンツの状態遷移、管理者権限、投稿ガイドラインを確定する。
7. 共有デッキ用のパッケージ形式をJSON Schemaで定義する。
8. PDFの容量上限、安全検査、削除申請フローを確定する。

最初のリリース目標は、**既存データを維持したまま「カード学習ホーム」と「テキスト思考メモ」を提供すること**とする。この2機能は公開ファイル運用に依存せず、最も早く利用者へ価値を届けられる。
