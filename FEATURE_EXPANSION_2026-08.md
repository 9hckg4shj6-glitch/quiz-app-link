# 機能拡張計画（2026年8月）

対象: `index.html`（問題一覧 qbrowse / 出題数モーダル / 学習画面）
状態: **4機能すべて完了（2026-08-28）**
最終更新: 2026-08-28

## 0. 依頼された4機能と、このアプリでの解釈

| # | 依頼 | このアプリでの意味 |
|---|---|---|
| 1 | 問題一覧から選んだ問題だけ一括演習 | `#qbrowse` の問題行にチェックを付け、選択した問題だけで `startQuiz` する |
| 2 | 問題一覧から見直し問題に指定 | 各行から `toggleBookmark(q.id)`（＝「見直したい問題」）を直接切り替える |
| 3 | まとめて演習の問題数指定 | 「▶ まとめて演習」を `startQuiz` 直呼びから `openCountModal` 経由に変える |
| 4 | 「この回を解く」で未着手を優先 | 既にある `unseenFirst` トグルが `playlist` モードで隠れているので出す |

3と4は**同じ出題数モーダルの話**なので、まとめて直す。1と2は問題一覧の行UIの話なのでまとめて直す。

## 1. 現状の関係コード

| 対象 | 場所 | 現状 |
|---|---|---|
| 問題一覧の一覧描画 | `index.html:4917` `renderQBrowseGroup()` | 10問/ページ。行は開閉と「▶ この問題を解く」だけ |
| まとめて演習 | `index.html:4926` | `startQuiz(items.slice(), g.label)` を直呼び。問題数を選べない |
| 見直し保存 | `index.html:2579-2582` `isBookmarked/toggleBookmark`、演習画面の `#bookmarkBtn`（`index.html:6643`） | 演習中しか付け外しできない |
| 出題数モーダル | `index.html:2593` `openCountModal()` | 「未定着だけ」「未着手を優先」の2トグルは `scopeable` のときだけ表示 |
| `scopeable` の条件 | `index.html:2599` | `year / prediction / special / field` のみ。**`playlist` が入っていない** |
| 「この回を解く」 | `index.html:5512` `startLessonRound()` | `openCountModal(..., "playlist")` を呼ぶ → 上記2トグルが出ない |
| 未着手優先の並び | `index.html` `sortByUnseen()` / `orderedPool()` | 実装済み（`seen` 昇順、同数はランダム） |

つまり **4は新規実装ではなく「既にある機能が学習画面から見えない」**。修正は表示条件1行が中心。

## 2. 実装方針

### 2-1. 機能4 + 機能3（出題数モーダル）

1. `scopeable` に `playlist` を追加する（`index.html:2599`）。
   これで「この回を解く」「経路の問題」「プレイリスト」でも
   「未定着の問題だけ出す」「まだ解いていない問題を優先」が選べるようになる。
2. `openCountModal` に `kicker` を任意で渡せるようにし、`startLessonRound` からは
   「この回を解く」を渡す（現状は一律「プレイリスト」表示）。
3. **既定値**: `playlist` かつ未着手が1問以上あるときは `unseenFirst` を既定ONにする。
   依頼4の「解いたことのある問題が繰り返し出る」への直接の答えになるため。
   トグルなのでOFFにすればこれまでどおり。
   （※この既定ONだけは挙動変更なので、不要なら実装時に外す）
4. 問題一覧の「▶ まとめて演習」を `openCountModal` に差し替える（機能3）。
   モードはグループ種別から決める。年度=`year` / 予想問題=`prediction` / 直前スペシャル=`special` /
   記述=`playlist`。`year` などを渡せば「続きから」も従来どおり復元できる。
5. アドホックな選択（機能1）では復元対象にならないので、
   `startFromCountModal()` の `ref` を `pendingSel.key ? {mode,key} : null` にして
   `meta.lastCategory` を汚さない。

差分の中心は `openCountModal` / `updateCountScope` / `startLessonRound` / `renderQBrowseGroup` の4か所。

### 2-2. 機能1（選んだ問題だけ演習）

- `qbrowseState` に `selected:new Set()` を追加。グループを切り替えたら破棄。
  ページをまたいでも選択は保持する（10問/ページのため必須）。
- 各行の先頭に選択チェックを置く（`.qbSel`）。`brHead` の開閉とは別ボタンにして
  `stopPropagation`。選択モードの切り替えはあえて作らず、常時表示にする（状態を増やさない）。
- 1問以上選ぶと下部に固定バー（`.qbSelBar`）を出す:
  `選択 N問` / `▶ 選択した問題を演習` / `選択解除`。
  「演習」は `openCountModal({items:選択, label:"<グループ名> ・ 選択した N問", key:null}, mode)`。
- 大問（`groupId`）は `startQuiz` の `expandGroupItems` が小問を連れてくるので、
  チェックは1問単位のままでよい。実際の出題数が指定数より増えうる点だけ注意。

### 2-3. 機能2（一覧から見直し指定）

- 各行の `.brMeta` に `☆ / ★` ボタン（`.qbBmBtn`）を追加し、
  `toggleBookmark(q.id)` → アイコン更新 → `toast` の3行で済ませる。
- 演習画面の `#bookmarkBtn` は大問をまとめて保存するが、一覧では**その1問だけ**を対象にする。
  一覧は問題単位の画面であり、まとめて付けたい場合は機能1の複数選択で足りるため。
- 既存の `status`（学習済／要復習）と並べて置く。

## 3. 変更するファイル

`index.html` のみ。

| 箇所 | 内容 |
|---|---|
| CSS `/* ===== 問題一覧（qbrowse） ===== */`（706行付近） | `.qbSel` `.qbBmBtn` `.qbSelBar` を追加 |
| `openCountModal` / `updateCountScope`（2593行付近） | `scopeable` に `playlist`、`kicker` 任意化、`unseenFirst` 既定 |
| `startFromCountModal`（2670行付近） | `key` が無ければ `catRef` を渡さない |
| `renderQBrowseGroup`（4917行付近） | 選択チェック、見直しボタン、選択バー、まとめて演習の `openCountModal` 化 |
| `qbrowseState` / `renderQBrowseIndex`（4802・4859行付近） | `selected` の初期化とグループ切替時クリア |
| `startLessonRound`（5512行付近） | `kicker` を渡す |

データ形式（`questions.js`、`progress`、Dexie、Supabase）は変更しない。
`bookmarked` は既存の `progress[id].bookmarked` をそのまま使うので同期も従来どおり。

## 4. 実装順序

1. 機能4 — **完了（2026-08-28）**。出題数モーダルのトグル2つ（未定着だけ／未着手優先）を廃止し、
   排他の2択「ランダムに演習する／まだ解いてない問題を優先」（`pendingSel.order`）に置き換えた。
   復習・試験日モード以外のすべての範囲で選べる。「この回を解く」は未着手が残っていれば既定で未着手優先
2. 機能3 — **完了（2026-08-28）**。問題一覧の「▶ まとめて演習」を `openCountModal` 経由にした。
   グループに `mode`（年度=year／予想=prediction／直前=special／記述=playlist）を持たせ、
   「続きから」の復元は従来どおり。`key` が無い範囲は `meta.lastCategory` に残さない
3. 機能2 — **完了（2026-08-28）**。各行の右端に ☆／★ ボタンを置いた。
   `brHead`（開閉ボタン）の中にボタンは入れられないので `.qbRow` で横に並べている
4. 機能1 — **完了（2026-08-28）**。行頭のチェックで選び、1問以上選ぶと下部の操作バーから
   出題数モーダルへ。選択はページをまたいで保持し、グループを離れると破棄。
   選んだ問題は初期値が「選んだ数」（`startAll`）、`key` を持たないので「続きから」には残らない

各段階で独立して動く。1と2で分けてコミットできる。

## 5. 確認方法

内容データを触らないので `validate:content` は不要。手動確認のみ:

- 学習 → 任意の回 → 「この回を解く」で2トグルが出る／未着手優先ONで未解答が先に出る
- 問題一覧 → 年度 → 「▶ まとめて演習」で出題数モーダルが出る／10問チップで10問だけ出る
- 問題一覧の行の ☆ を押す → ホームの「見直したい問題」の件数が増える
- 3問チェック → 「選択した問題を演習」→ その3問だけ出る（大問を含むときは小問が付いてくる）
- ページを2→1に戻しても選択が消えない
- 既存の「続きから」が壊れていない（年度からのまとめて演習後にホームで確認）

## 6. リスク

| リスク | 対応 |
|---|---|
| `unseenFirst` 既定ON が「同じ回を復習したい」用途と衝突 | トグルでOFFにできる。不要なら既定OFFに戻す |
| 選択＋出題数指定で「選んだのに全部出ない」混乱 | 選択から開いたときは入力欄の初期値を選択数にする |
| 大問により実際の出題数が指定数を超える | 従来から同じ挙動。モーダルの注記で触れない（既存踏襲） |
| 一覧の行にボタンが増えて窮屈 | ☆ は `.brMeta` 内のアイコンのみ、チェックは行頭の小さな枠に留める |
